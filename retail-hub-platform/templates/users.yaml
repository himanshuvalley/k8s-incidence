apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "retail-hub.fullname" . }}-users
  labels:
    {{- include "retail-hub.labels" . | nindent 4 }}
    app.kubernetes.io/component: users
spec:
  replicas: {{ .Values.users.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "retail-hub.name" . }}
      app.kubernetes.io/component: users
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "retail-hub.name" . }}
        app.kubernetes.io/component: users
    spec:
      {{- include "retail-hub.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: users
          image: "{{ .Values.users.image.repository }}:{{ .Values.users.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports: [{ name: http, containerPort: {{ .Values.users.port }} }]
          env:
            - name: PORT
              value: {{ .Values.users.port | quote }}
          envFrom:
            - secretRef:
                name: {{ include "retail-hub.fullname" . }}-mongodb-app
          resources:
{{ toYaml .Values.users.resources | indent 12 }}
          startupProbe:
            httpGet: { path: /ready, port: {{ .Values.users.port }} }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /ready, port: {{ .Values.users.port }} }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: {{ .Values.users.port }} }
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-hub.fullname" . }}-users
  labels:
    {{- include "retail-hub.labels" . | nindent 4 }}
    app.kubernetes.io/component: users
spec:
  selector:
    app.kubernetes.io/name: {{ include "retail-hub.name" . }}
    app.kubernetes.io/component: users
  ports:
    - port: {{ .Values.users.port }}
      targetPort: http
      name: http
