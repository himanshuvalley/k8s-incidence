apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-fulfillment-worker.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "order-fulfillment-worker.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "order-fulfillment-worker.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
